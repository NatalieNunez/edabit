function isFirstSuperior(arr1, arr2) {
	return arr1 > arr2 ? true : false;
}
// instructions: Create a function that returns whether the first array is slightly superior to that of the second.
// input: two arrays
// ouput: boolean
// if arrays are the same, return false
// ternary operator maybe?