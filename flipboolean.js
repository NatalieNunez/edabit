function flipBool(b) {
	return b === true || b === 1 ? 0 : 1;
}
/* function flipBool(b) {
	return b ? 0 : 1; 
 } 
 since its a boolean it will already return a truthy value so theres no need to set equal to true or 1 */
