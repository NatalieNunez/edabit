function arrayToString(arr) {
  let str = '';
    for (let i = 0; i < arr.length; i++) {
      str = str + arr[i];
    }
    return str;
  }
  /* function arrayToString(arr) {
    return arr.join('');
  } */