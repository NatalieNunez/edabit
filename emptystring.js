function isEmpty(s) {
	return s === "";
} // refactored; no need to use ternary, booleans will already return true or false
/* function isEmpty(s) {
	return (s === "" ? true : false);
}
 function isEmpty(s) {
	return s.length === 0 ? true : false;
 } */