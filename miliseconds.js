function past(h, m, s){
  return (1000*(h*3600000)+(m*60000)+s);
}
// Clock shows 'h' hours, 'm' minutes and 's' seconds after midnight.
// Your task is to make 'Past' function which returns time converted to milliseconds.