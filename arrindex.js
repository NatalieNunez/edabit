function valueAt(arr, i) {
	return arr[Math.floor(i)];
}
// input array and number
// output number within array 
// Math.floor(i)

// instructions: Given an index and an array, return the value of the array with the given index.
// example: valueAt([1, 2, 3, 4, 5, 6], 10 / 2) ➞ 6