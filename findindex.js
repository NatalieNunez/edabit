function search(arr, item) {
  for (let i = 0; i < arr.length; i++) {
   return arr.indexOf(item);
  } 
   }
   /* function search(arr, item) {
	if (arr.includes(item)) {
		return arr.indexOf(item);
} else {
	return -1;
 }
} */