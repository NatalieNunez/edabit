function area(h, w) {
	if (h <= 0 || w <= 0) {
		return -1;
	} else {
		return h*w;
	}
}
// input two numbers output one number
// if numbers are positive, solve equation
// if one or more numbers are negative return -1
// use || or 
// if else statement